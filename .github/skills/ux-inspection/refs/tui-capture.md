# Terminal and CLI Capture Notes

Use terminal capture for CLI summaries, replay output, and local workflow debugging.

- Capture the output before and after the change.
- Include the run ID, selected action, risk decision, and reconciliation status when relevant.
- Save artifacts with scenario-specific names so they are easy to compare.
