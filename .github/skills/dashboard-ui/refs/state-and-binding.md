# State and Binding Guidance

Use explicit state models so the UI reflects the same run, risk, and ledger data that the workflow uses.

- Bind to stable state objects rather than ad hoc view models.
- Keep source metadata and risk decisions part of the display model.
- Avoid duplicating business logic in the UI layer.
